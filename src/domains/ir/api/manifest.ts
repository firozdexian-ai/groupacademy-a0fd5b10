/**
 * IR domain API manifest. No edge functions yet — reserved for future
 * (e.g., investor digest emails, automated KPI snapshots).
 */
export const irApi = {} as const;

export type IrApi = typeof irApi;

